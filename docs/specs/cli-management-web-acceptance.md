# CLI 管理 Web 验收

## 独立 CLI 补丁发布验证（2026-09-10）

- 基线为上游 `0c1d10a1`，在独立 `feat/cli-management-upstream` 分支整理。功能不依赖 Mattermost 连接器或治理文档合入；公共 `automation_schedule.rs` 无差异，原生语音、权限及三语设置的上游改动保留。
- 本轮重新执行：核心 214、Runtime 命令/探测 11、DeepSeek 10、CLI 管理 25、Actor 3、原生锁策略 8、分发 5、Claude 语音会话 52、HTTP/Runtime 5、原生认证 10、前端组件 18、三语键名/占位符 2，共 **363 项通过、0 项失败**。真实供应商安装测试 1 项按原规则忽略；子进程及重复运行不重复计数。
- 补跑上游 CI 使用的 `npm run check`，发现本功能三个新增 TSX 文件未通过格式检查；只使用原生格式工具处理这三个文件，未改变功能逻辑或其他页面。之后原生格式/lint/TypeScript 检查、18 项组件回归及生产构建通过。改动 Rust 文件的格式检查与 Git 空白检查通过。
- `cargo build -p cccc --bin cccc` 通过。以同一构建启动独立临时 Home、清空继承环境、仅监听本机；未复制供应商登录或业务数据，未部署服务器。真实浏览器验证 17 个 CLI 与原始目录 19 个 Runtime、未受管时更新/卸载禁用、缺少 mise 时失败记录和日志正确、默认 03:00 未启用、周日计划启用/保存/页面重载/移除，以及 390px 窄屏无横向溢出。
- 周日计划经网页保存为 `0 3 * * 0`、`Asia/Shanghai`，当时下次执行为 `2026-09-12T19:00:00Z`，网页对应周日 9 月 13 日 03:00；移除后规则数组为空。一次预期失败操作保留创建/开始/结束时间及错误，受管安装始终为空。用浏览器 DOM、只读 HTTP 和截图交叉核对，不用仅返回 Done 的屏幕外点击或等待超时冒充成功；显式滚入可视区域后操作通过，未为自动化工具修改产品控件。
- 本节的验证命令除下方已有入口外，增加 `cargo test -p cccc-pair-daemon --lib codex_voice_analyst::claude::`、`cargo test -p cccc-pair-web --lib auth::tests`、`python3 -m unittest discover -s tests -p test_web_i18n_locales_parity.py`。这不是全仓测试或跨平台验收；先前供应商安装和部署矩阵仍仅作历史证据。
- 术语检查：通过；受管安装、CLI 更新、CLI 自动更新计划、CLI 操作记录与 Runtime/Actor/原生会话边界保持一致。功能指南和规格的本地链接有效，不依赖未合入治理分支的文件。

## 故障隔离与上游整理复测（2026-09-10，未部署）

- Runtime 目录读取管理状态失败时保留原生可用性、路径、推荐命令和可用列表，另报管理错误；实际启动仍拒绝坏状态。回归用隔离子进程提供确定可用的外部 Codex，逐字段对比原生探测，并检查坏状态未改写、恢复后错误消失。
- DeepSeek 新增公共入口与旧入口的等价性测试：管理状态不存在/合法空状态、默认/显式代理设置、已就绪结果、环境、配置及原生 Node 失败一致；原有安装/迁移/重试测试保留。受管只读检查的 false/false 表示本次未安装/创建，不改变 setup JSON 含义。
- 文档治理独立提交 `591be880`；CLI 功能代码未提交。新基线整理在 `feat/cli-management-upstream`，基于上游 `dd556f1b`，不夹带 Mattermost 或治理文档。旧纯 CLI 分支与集成区保留，并同步上述故障隔离修正。
- 本工作区采用新上游的操作注册接口；CLI 管理的 2 个读取、3 个写入操作声明原生 Policy，不修改公共调度算法和已有操作策略。
- 已完成：新基线核心库 214 项、DeepSeek 10 项、CLI 管理 25 项、Actor 3 项、原生锁策略 8 项、原生分发 5 项、Runtime 命令/探测 11 项、HTTP/Runtime 5 项、前端组件 18 项，共 **299 项通过、0 项失败**。供应商真实安装测试 1 项按原规则忽略，不计入通过数；子进程重复报告不重复计数。旧基线另跑 DeepSeek 10 项，不计入新基线总数。
- 新基线前端生产构建、TypeScript、原生 lint、改动 Rust 文件格式及 Git diff 空白检查通过。构建有既有体积/耗时提示，不是测试失败。当前 WSL 缺少的 OpenSSL 开发文件采用本地隔离构建依赖，没有系统安装或服务器改动。
- 术语检查通过：受管安装、CLI 更新、Runtime、Actor、原生会话含义未变；保留旧 `upgrade` 记录兼容说明，不再混用“纳管”状态。治理规则的集成/治理副本逐文件一致；纯 CLI 分支不夹带治理文档。
- 本轮没有真实浏览器/部署验收，没有推送或创建 PR；不将旧的 255 项和下方 Web 矩阵当作本轮证据。功能代码保持未提交，治理提交独立。

本轮自动化入口：`cargo test -p cccc-pair-core --lib`；Daemon 的 `deepseek_setup`、`cli_management`、`managed_cli`、`dispatch_concurrency`、`dispatch::tests` 五个筛选；`cargo test -p cccc-pair-runtime --lib command::tests`；Web 的 `--test runtimes --test cli_management`；前端入口同下文。Daemon 后四组使用本轮刚编译的同一测试二进制执行，不使用旧产物。所有供应商真实安装/模型调用均不在本轮复测范围。

## 前次审阅修订复测状态（2026-09-10，部署历史）

本节是前次审阅整改的历史状态。下面完整 Web 矩阵也是此前证据；不得据此推断顶部后续补丁或新基线已部署、已完成真实 Web 验收。

- 已修改：Worker 有界关闭及进程回收测试；Actor 集成测试移到同级文件；锁中毒策略注释；Runtime 目录在 CLI 状态损坏时降级而启动仍拒绝；受管/更新术语统一；规格拆成当前有效合同与历史记录。
- 公共 `automation_schedule.rs` 与整改前基线相同；星期转换仅在 CLI 管理内部，保存的表达式不变。
- 已通过：核心库 205 项、Daemon CLI 管理 24 项、Actor 集成 3 项、HTTP/Runtime 5 项、前端组件 18 项，共 255 项；另 1 项供应商测试忽略、不计入。TypeScript、原生 lint、生产构建、格式检查通过。
- 实际页面验证：17 个 CLI；受管/更新术语；旧操作记录显示；每周日 03:00 的创建、启用、持久化及正确下次时间；移除计划；隔离坏状态下 Runtime HTTP 200 降级和页面报错；修复后自动恢复、记录保留。原生操作、HTTP 与文件状态交叉核对。
- 开发部署程序/网页哈希匹配，实际页面复核通过；既有 Actor/桥接与管理状态保持，标准对照未修改或重启。隔离测试实例退出码 0，临时隧道关闭。此前审批阻塞已经用户批准解除。
- 文档治理与纯 CLI 功能已拆成独立分支工作区，部署组合使用集成分支；均未提交、推送或创建 PR。纯 CLI 分支独立构建及同组前端测试通过，重复测试不计入上述总数。
- 工具故障、初次构建依赖/沙箱错误及修正后复测分别留证，不当作产品通过；详细部署目标和证据仅放内部运维资料。

## 前次发布验收记录

日期：2026-09-10。目标为本地开发源码及同一产物的开发测试部署。下列 17 组用例已完成；真实浏览器、后台集成和供应商分发验证的范围分别记录，不互相替代。

后续术语修订：CLI 自动更新计划及统一 `update` 的修改已通过 56 项自动化回归，并完成配套开发部署及真实浏览器复核。新增覆盖旧记录显示为“更新”、新旧请求兼容且去重、17 个更新按钮、原生未启用 03:00 计划新增/保存/移除；下列完整矩阵保留前版历史证据，未冒充全部重新执行。详见 [最新规格](cli-management.md)。历史“升级”是旧称，不代表新实现仍使用该业务术语。

## 方法与隔离

- 真实浏览器使用 agent-browser 驱动原生页面；组件测试用现有 Vitest，后台集成用现有 Rust 测试。每层分别留证，不互相冒充。
- 破坏性、失败和版本切换场景使用独立 Home、工作组、外部 CLI 与 mise 夹具，不挂载业务登录；夹具执行真实进程、真实文件安装与删除，不调用供应商模型。
- Web 中提交操作、启动/停止 Actor；后台读取状态、进程与文件证据做交叉核验。预置夹具、故障注入可以使用脚本，不能用 API 代替标注为 Web 的用户步骤。
- 发布实例核验同一程序/网页哈希、页面、持久化、健康及既有 Actor/桥接；标准对照不得改变。
- 软件分发逐供应商验证独立于上述通用流程；供应商登录和模型推理不属于本次 CLI 生命周期验收。

## 用例清单

| 编号 | Web 操作及预期 | 交叉核验 | 状态 |
|---|---|---|---|
| W01 | 全局 CLI 管理入口、17 项可管理 CLI，不显示 Web Model / Custom；专用错误报告消失 | 后端保留 19 项 Runtime；旧通知配置不再读取、接口无配置作用 | 通过：2026-09-10 修订后 16 项组件测试、开发实例真实 DOM/截图及 Runtime API 复核 |
| W02 | 仅外部安装时来源与路径正确，不能升级/卸载；安装受管副本后成功可用 | 真实目录、版本探针、外部校验值不变 | 通过：Web 安装独立副本 |
| W03 | Web 启动默认 Actor，实际使用受管 CLI；升级时旧 Actor 不被中断，新启动使用新版本 | 实际进程路径、PID、版本输出，不只看 PATH/配置 | 通过：实际 PTY、PID、1.0.0→2.0.0 |
| W04 | 升级成功、无新版本结束、验证失败及损坏后的修复均正确显示 | 安装选择、文件、错误和日志一致，失败保留旧版本 | 通过：失败退出 7；移走文件后 Web 升级修复 |
| W05 | 卸载原生确认：取消零操作；活动 Actor 时拒绝，停止后可卸载 | 使用锁、任务结果；仅删除所属受管目录 | 通过：真实确认、取消及后台保护 |
| W06 | 卸载后 Web 再启动默认 Actor，实际使用外部版本；无外部时不可用 | 路径及版本、外部文件/链接/权限/登录夹具前后校验一致 | 通过：实际外部 0.9.0；无外部时启动失败 |
| W07 | 显式指定程序路径的 Actor 不被默认受管选择改写 | 实际进程及版本 | 通过：始终使用指定外部脚本 |
| W08 | 添加计划默认 03:00 未启用；编辑、启停、删除后刷新一致 | 保存状态、revision、时区和下次触发 | 通过：网页增删、启用与禁用 |
| W09 | 间隔、每天、每周/周日、每月/月末、一次性、复杂 cron 保留及时区 | 原生控件保存值；非法输入拒绝、草稿保留 | 通过：Web 保存值；月末计算另由原生调度回归验证 |
| W10 | 计划到期、关闭页面仍执行；多 CLI 排队，只有一项运行；卸载后的 CLI 不再升级 | source_rule、时间区间、后台持久化，不只看保存成功 | 通过：两次实际一次性触发；后一次仅剩 Codex |
| W11 | CLI 操作记录名称、对象、类型、来源、五种状态、三个时间正确；刷新和重启不丢 | 逐条与后台记录和真实文件结果比较 | 通过：26 条记录；运行、排队及重启后的中断状态分别留证 |
| W12 | 重复点击和网络重试不重复执行；最近20条/全部切换正确 | 操作编号、执行次数、记录总数 | 通过：Web 双击与断网重试；HTTP 同编号重放另有回归 |
| W13 | 不同操作日志不串线；翻页、刷新、脱敏、失败重试正确 | 操作编号、stdout/stderr/退出结果、合成敏感值不外露 | 通过：成功/失败日志各 9 页；上一页、读取断网及恢复 |
| W14 | 断网、过期计划保存冲突、后台故障及恢复明确反馈；中断操作不假报成功 | 草稿、错误码、磁盘状态及重启后队列 | 通过：实际进程中断、状态文件损坏/恢复、409 和断网 |
| W15 | 管理员正常；受限身份看不到可操作的 CLI 管理页面，直接请求被拒绝 | 原生身份/CSRF 边界与 HTTP 回归 | 通过：原生受限令牌/管理员对照；查询与三类写入均拒绝受限身份 |
| W16 | 中英日、明暗、窄屏、滚动和键盘可用，原生确认不被替换 | 页面截图、可见文字、焦点及横向溢出检查 | 通过：390px 页面无横向溢出；原生键盘与分段日期输入 |
| W17 | 开发测试部署页面呈现最新功能、刷新后可用，既有状态保持 | 源码/程序/网页哈希、容器健康、标准实例未变 | 通过：部署健康、页面和既有 Actor/桥接状态核验 |

## 可重复自动化入口

- 组件：`cd web && npm test -- src/components/modals/settings/CliManagementTab.test.tsx`。
- 状态/安装/卸载：`cargo test -p cccc-pair-core -p cccc-pair-daemon --lib cli_management`；真实供应商测试默认忽略，必须在获准无凭据实验环境单独运行。
- HTTP 与 Runtime：`cargo test -p cccc-pair-web --test cli_management --test runtimes`。
- Actor 集成：`cargo test -p cccc-pair-daemon managed_cli`。
- 浏览器执行与部署证据在内部验收记录维护，不向公开仓库放入真实服务器信息和账号数据。

## 本轮发现与修正

- 原记录缺少开始/结束时间和明确手动/计划标签：补齐展示，保留后台 `Job`/`jobs` 协议。
- 删除 URL 后原生 Web fallback 返回 HTML 200，不是配置成功；回归检查响应类型、旧配置不变及 Daemon 操作不再处理，不为这个已删除功能新增专属路由。

## 执行结果与边界

- 核心 8、Daemon 22、Actor 实际启动 3、HTTP/Runtime 4、前端 15，共 52 项回归通过；默认忽略的真实供应商安装测试 1 项不计入通过数。本次未重新下载全部供应商软件，既有逐供应商安装证据见功能规格。
- 26 条浏览器操作记录包含手动和计划来源；开始/结束区间无重叠。将后台 ISO 时间按浏览器相同的 `toLocaleString()` 转换后逐项核对页面，不仅检查时间先后关系。
- 杀停无凭据隔离实例时，一项运行、一项排队。重启后运行项成为 `interrupted`，排队项继续成功，旧成功记录未重放。存储损坏时出现明确错误，恢复原文件后错误消失且记录不丢。
- 浏览器网络故障注入使用精确 URL；未命中的通配表达式不算断网通过。日期控件采用原生分段输入并读取实际值；仅自动化工具返回 Done 不算成功。工具等待、过期引用及文案大小写错误已按真实页面重试核验，没有修改产品控件来迁就工具。
- 受限身份仍可使用原生允许的个人配置页面；本功能只要求全局 CLI 管理不可访问，不改变 CCCC 既有权限体系。
- 仅部署获准的开发实例；没有安装或升级业务 CLI、启用业务自动计划、发送聊天或调用模型。供应商登录、真实模型推理及其他平台不在本次生命周期回归范围内。

术语检查：通过。CLI 操作记录是软件管理执行记录，不是协作任务（Task）；运行时（Runtime）、智能体（Actor）、CLI 安装/升级/卸载及自动升级计划沿用现有定义。
