# CCCC Mattermost 连接器领域用语

本文只定义通用 IM 接入用语，不规定工作组如何组织业务活动。

**工作组（Group）**：CCCC 管理一组智能体及其协作记录的范围。外部聊天可以订阅这个范围，但不因此生成另一份上下文。
_避免_：会议、Topic、Mattermost 频道。

**智能体（Actor）**：CCCC 工作组中的参与者，有自己的稳定身份、角色及运行时。
_避免_：接入机器人账号、固定的模型品牌。

**协调者（Foreman）**：CCCC 工作组中的默认消息收件角色，由工作组自身配置。
_避免_：连接器内置的主持人。

**接入机器人（Bot）**：在 Mattermost 代表一个 CCCC 工作组收发消息的服务身份，不对应某一个 Actor。
_避免_：用一个 Bot 身份表示多个未经区分的工作组。

**聊天目标（Chat Target）**：可以独立申请接入的外部聊天位置，包括频道、直接消息或指定线程。
_避免_：独立模型 Session。

**聊天授权（Authorization）**：CCCC 允许一个聊天目标访问该工作组的决定。
_避免_：把聊天授权称为完整的逐用户权限系统。

**订阅（Subscription）**：聊天目标接收该工作组消息的设置，包括暂停及详细交流选项。
_避免_：把同组多个订阅称为隔离的上下文。

**配对申请（Pending Request）**：尚未批准且有有效期的聊天接入请求，等待有权操作者在 CCCC 确认。
_避免_：登录令牌、已批准的授权。

**详细交流（Verbose）**：在正常面向用户的消息之外，允许向该订阅显示符合公开条件的成员间消息。
_避免_：公开全部系统信息和终端输出。

**暂停桥接（Pause）**：暂时暂停该聊天目标的消息传递，不停止 Actor 或重置工作组。
_避免_：取消模型任务、结束会议。
